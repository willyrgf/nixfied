# Detailed Notes

- Scenario ownership is defined in `proof-workspace/scenarios/coverage-map.nix`.
- Runtime behaviors are proven through scenario checks and coverage-map gates.
- The seed's `nixfied/project/` and `nixfied/local/` trees are intentionally minimal and proof-focused.
