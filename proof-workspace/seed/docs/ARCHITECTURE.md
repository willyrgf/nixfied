# Proof Workspace Architecture

The proof workspace is a repository-shaped fixture that exercises the framework through public surfaces.

Bootstrap may either copy this seed into a source-backed temp repo or overlay it onto an installed wrapper, but the project behavior stays owned by the checked-in files in this directory. Framework-owned files are supplied by the source repo or temp install/upgrade materializations, not by the checked-in seed.
