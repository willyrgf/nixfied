# Proof Workspace Architecture

The proof workspace is a repository-shaped fixture that exercises the framework through public surfaces.

Bootstrap may either copy this seed directly or overlay it onto an installed wrapper, but the project behavior stays owned by the checked-in files in this directory.
