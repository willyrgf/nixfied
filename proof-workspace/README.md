# Proof Workspace

This directory contains the canonical repository-shaped fixture used by proof-workspace scenarios.

Layout:

- `seed/`: checked-in seed workspace materialized into temporary git repositories.
- `scenarios/`: scenario checks and ownership metadata.
- `lib/`: deterministic bootstrap and assertion helpers shared by scenario checks.
