nixfied capability descriptor
model-version: 1

# The wire primitives the runtime admits and lowers. A field added or removed
# here is a contract change; editing this manifest rotates the runtime ABI digest
# so models emitted against the old contract are rejected by the identity check.
primitive Model: modelVersion toolchainId runtimeAbi generator project target codebases secrets environments slotPolicy placement state closures services tasks docs
primitive Generator: name version emitter
primitive Project: projectId name
primitive Target: system os arch closureSystem
primitive Codebase: codebaseId logicalRoot sourceMode sourceIdentity sourcePolicy
primitive SourcePolicy: dirtyPolicy admissionFingerprintPolicy
primitive StatePolicy: markerIdentity stateEpoch cleanupPolicy persistence
primitive SecretDescriptor: secretId source
primitive SecretSource: kind envVar path
primitive SlotPolicy: min default max
primitive Placement: slotPlacements
primitive SlotPlacement: slot candidatePorts
primitive CandidatePortWindow: start end
primitive ClosureSpec: kind storePath executable targetSystem operationBindings requiresExecutable effects
primitive Invocation: tools run executable env codebaseId cwd stdin timeoutMs
# endpoints/primaryEndpoint are optional: durable is not listening. An
# endpoint-less service makes no addressability claim — invocation probes only,
# nothing may address it, no port demand.
primitive ServiceSpec: lifecycle endpoints primaryEndpoint connectsTo stateRefs logRefs containment
primitive Lifecycle: prepare start ready health stop clean
primitive PrepareSpec: task
primitive StartSpec: operationId invocation terminal
primitive ReadySpec: operationId probe terminal
primitive HealthSpec: operationId probe terminal
primitive StopSpec: operationId signal timeoutMs terminal
primitive CleanSpec: operationId terminal
primitive Endpoint: endpointId host
primitive ProbeSpec: kind invocation timeoutMs retryIntervalMs maxAttempts
primitive TerminalSemantics: success failure
primitive TaskSpec: kind serviceLifetime operationId invocation requires servicesRequired exitPolicy steps artifactRefs logRefs summaryRefs
primitive StepSpec: task dependsOn
primitive ExitPolicy: successCodes
primitive Docs: title summary

# The invocation arg/env placeholder grammar the runtime substitutes. Bare forms
# resolve to the invocation's own primary endpoint (services) or the primary
# dependency (tasks). The named form ${port:<name>} resolves <name> first against
# the owning service's endpoint ids, then against a service declared via
# connectsTo (services) or requires (tasks) — its primary slot-plan endpoint.
substitution: ${port} ${host} ${stateDir} ${port:<name>} ${host:<name>} ${secret:<id>}

# The runtime command surfaces every model exposes.
surface: model schema docs capabilities check run ps down clean

# The run command output projections. Summary is human-facing; json is the
# structured automation surface; both is the explicit combined diagnostic mode.
run-output-mode: summary json both

# The runtime-owned run output schemas. These JSON/text projections are public
# for the current ABI, same as the admitted model and error vocabulary; changing
# required fields or field meaning records a contract change here and rotates the
# runtime ABI digest.
output-schema run-json: runId modelPath computedModelHash durationMs services tasks task? summaryPath? nodes runSummaryPath?
output-schema run-summary-json: runId success durationMs services nodes tasks
output-schema run-task: taskId stepPath processKey exitCode timedOut canceled success durationMs stdoutPath stderrPath summaryPath
output-schema run-node: nodeId taskId success exitCode durationMs stdoutPath stderrPath summaryPath
output-schema run-service: serviceId serviceInstanceId processKey selectedEndpoint?
output-schema selected-endpoint: endpointId host port
output-schema ps-json: processes
output-schema ps-process: processKey runId serviceInstanceId? pid pgid registryStatus reconciledStatus serviceLifetime? borrowerCount live
output-schema runtime-error-port-conflict: portConflict
output-schema port-conflict: reason projectId endpoint nixfiedOwner?
output-schema port-conflict-endpoint: transport family address port endpointId
output-schema nixfied-owner: projectId environment slot runId serviceId serviceInstanceId processKey
output-schema run-summary-text: slot-upgrade service-start prepare-node service-ready-endpoint service-ready-endpointless task-ok task-fail task-fail-stderr result-ok result-fail-empty result-fail-nodes run-summary logs
output-schema run-error-summary-text: error

# Endpoint locks serialize only service-specific mutation and are released after
# the atomic ready transition. Kernel listeners remain steady-state ownership.
# Reuse requires exact listener/process/containment/endpoint proof and a guarded
# registry CAS. Linux observes SOCK_DIAG; macOS observes pcblist_n.
endpoint-acquisition: fixed-euid-startup-lock exact-bind-preflight linux-sock-diag macos-pcblist-n socket-steady-state
endpoint-reuse: exact-listener process-identity containment complete-active-endpoint-set guarded-cas
lease-authority: open-lease-blocks-replacement exact-reuse-adds-borrower expired-processless-reservation-reconciled
escape-settlement: exact-process-identity expected-active-prestate atomic-process-run-event ports-retained affected-row-validation
escaped-port-reconciliation: escaped-plus-open-port unresolved ps-visible down-actionable cleanup-blocked release-after-death-proof

# The closed stop signals the runtime can send.
signal: TERM INT QUIT HUP

# The closed model enum domains and their wire values. Editing a variant is a
# contract change that rotates the ABI digest, same as a primitive field.
enum ClosureKind: executable helper
enum ClosureEffect: process network-listener source-read file-write
enum StdinPolicy: null inherit
enum ProbeKind: tcp exec
enum TaskKind: leaf composite
enum ServiceLifetime: run-scoped until-idle persistent-until-down
enum SecretSourceKind: env-var file
enum ContainmentRequirement: process-group process-tree
enum SourceMode: snapshot flake-input live-workspace
enum DirtyPolicy: allow warn reject
enum CleanupPolicy: delete-on-clean protected
enum PersistencePolicy: run-scoped persistent

# The typed registry status domains and their wire values.
status RunStatus: service-starting canceling canceled completed task-succeeded task-failed service-failed proc-escaped stale
status ProcessStatus: starting running ready stopped succeeded failed canceled escaped stale
status RunLeaseStatus: active canceling canceled completed failed stale
status PortStatus: reserved active released stale
status CleanupStatus: intent deleted failed

# PORT_CONFLICT is constructed only from one of these two proven host facts.
enum PortConflictReason: startup-lock-contended listener-occupied

# The public runtime error contract: the stable error codes and exit classes the
# runtime emits. These are public API for the current ABI, so a change rotates the
# digest like any primitive field — old models and the new runtime cannot disagree
# on the failure vocabulary without the identity check noticing.
error-code: MODEL_NOT_STORE_OUTPUT MODEL_INVALID MODEL_ADMISSION RUNTIME_ABI_MISMATCH SOURCE_MISMATCH PLATFORM_UNSUPPORTED CLOSURE_MISSING REGISTRY_CORRUPT STATE_UNWRITABLE STATE_UNOWNED CLEANUP_REFUSED PORT_CONFLICT PORT_UNVERIFIABLE PROC_ESCAPE READINESS_TIMEOUT CANCELED LEASE_STALE LEASE_CONFLICT TASK_FAILED LIFECYCLE_FAILED DEPENDENCY_UNAVAILABLE SECRET_UNAVAILABLE SECRET_LEAK_BLOCKED
exit-class: ok error
