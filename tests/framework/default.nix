{
  pkgs,
  model,
  services,
  serviceDefinitions,
  serviceCatalog,
  stateHash,
  canonical,
  registry,
  packages,
  apps,
}:
let
  frameworkTestCatalog = import ./framework-test-catalog.nix;
in
frameworkTestCatalog.mkChecks {
  inherit
    pkgs
    model
    services
    serviceDefinitions
    serviceCatalog
    stateHash
    canonical
    registry
    packages
    apps
    ;
}
